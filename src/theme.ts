import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: 'Helvetica, sans-serif',
  headings: {
    fontFamily: 'Microgramma, Greycliff CF, sans-serif',
  },
});
